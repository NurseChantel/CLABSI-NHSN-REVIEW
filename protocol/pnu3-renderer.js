const escapeHtml = value => String(value).replace(/[&<>'"]/g, character => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character]);
const row = (label, value) => `<div class="protocol-row"><strong>${escapeHtml(label)}</strong><span>${escapeHtml(value)}</span></div>`;
const section = (title, body) => `<section class="protocol-section"><h4>${escapeHtml(title)}</h4>${body}</section>`;

export function renderCompactPnu3({ evaluation, patientContext }) {
  if (!evaluation || typeof evaluation.met !== "boolean") throw new TypeError("A valid PNU3 evaluation is required");
  const needed = evaluation.remainingRequirements.map(item => `<li>${escapeHtml(item.message)}</li>`).join("");
  const hosts = evaluation.hostEligibility.branches.filter(item => item.met).map(item => row(item.id, "Met")).join("") || "<p>No host pathway is currently complete.</p>";
  const labs = evaluation.laboratoryEvidence.branches.filter(item => item.met).map(item => row(item.id, "Met")).join("") || "<p>No laboratory branch is currently complete.</p>";
  const status = `<div class="secondary-site-status ${evaluation.met ? "met" : "incomplete"}" role="status"><strong>${escapeHtml(evaluation.status)}</strong>${needed ? `<span>Still needed:</span><ul>${needed}</ul>` : ""}</div>`;
  return `<div class="evidence-group secondary-evidence-review" data-protocol-renderer="compact" data-protocol="PNU3">${section("Patient Context", row("Date of birth", patientContext.dateOfBirth ?? `${patientContext.exactAge.value} ${patientContext.exactAge.unit}`))}${section("Host Eligibility", hosts)}${section("Timeline", row("Infection Window Period", evaluation.infectionWindow ? `${evaluation.infectionWindow.start} through ${evaluation.infectionWindow.end}` : "Not established") + row("Date of Event", evaluation.dateOfEvent ?? "Not established") + row("Repeat Infection Timeframe", evaluation.repeatInfectionTimeframe ? `${evaluation.repeatInfectionTimeframe.start} through ${evaluation.repeatInfectionTimeframe.end}` : "Not established"))}${section("Imaging", row("Requirement", evaluation.imaging.met ? "Met" : "Not met"))}${section("Clinical Criteria", row("One qualifying finding", evaluation.clinical.met ? "Met" : "Not met"))}${section("Laboratory Evidence", labs)}${section("Specimen Eligibility", "<p>Eligibility is evaluated by specimen type, contamination status, collection route, method, and organism.</p>")}${section("Exclusions", `<p>${escapeHtml(evaluation.exclusions.join(", "))}</p>`)}${section("PNU3 Status", status)}${section("Secondary BSI Attribution", row("Status", evaluation.secondaryBsi.met ? "Met" : "Not met") + row("Organism relationship", evaluation.secondaryBsi.organismRelationshipMet ? "Met" : "Not met") + row("Attribution timing", evaluation.secondaryBsi.timingMet ? "Met" : "Not met"))}</div>`;
}

export function renderPnu3Safely(options) { try { return renderCompactPnu3(options); } catch { return '<div class="secondary-guidance warning" role="status">PNU3 guidance could not be loaded.</div>'; } }
